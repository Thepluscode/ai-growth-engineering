# Digital Marketing Project — Feature and Market Tracker

Last updated: 2026-09-08

| Capability / outcome | Status | Evidence / next gate |
| --- | --- | --- |
| Project identity and scope | VERIFIED | Digital Marketing is the project; AI Growth Engineering is the method and engineering layer; `docs/ARCHITECTURE.md` defines the eight operating areas |
| Full-stack architecture of record | VERIFIED | `capability_map.json` declares 180 capabilities across all eight domains; semantic coverage tests prevent the source scope from silently collapsing back to the outbound wedge |
| Twenty-two software-memory registries | DEPLOYED | Twenty generated and two specialised registries now include product opportunities and product-format decisions; typed schemas, write/read interfaces, durable seeds and in-place migration are tested |
| Product build gate | DEPLOYED | Non-compensatory code requires buyer, problem, unique evidence threshold, demand signal, distribution path, measurable purchase action, validation test and economics before scoring; high margin cannot override missing evidence |
| Product format decision model | DEPLOYED | Deterministic tests choose static asset, interactive tool, manual service, software or subscription from recurrence, data, state, automation, integration and ongoing value |
| Product opportunity portfolio | VERIFIED | Five existing offers loaded from durable seeds; all five return RESEARCH and zero are ranked. `intelligence/product-opportunities/PORTFOLIO.md` separates observed, inferred and unknown rather than padding the portfolio to 20–30 ideas |
| Opportunity Graveyard | DEPLOYED | Parking preserves the idea, reason, date and observable reopen condition; current graveyard is empty because no product-level purchase test has failed |
| GrowthOps operator | VERIFIED LOCAL | Deterministic operator reads scoreboard, route splits, experiment state, evidence and product eligibility, then proposes exactly one next action; it can persist sourced teardown/draft workflow state but has no external send, spend or publish interface. The time-since-contact freeze was removed on 2026-08-30: it was the first branch in `_next_move`, so it shadowed every other diagnosis, and with no sends going out it stayed permanently red — wallpaper rather than a rule. It had been masking `ACCESS CONSTRAINT` (50 delivered sends, 0 meaningful replies), which is the operator's real reading of this funnel. Elapsed time since last contact is still reported; it is no longer a verdict. Putting the freeze back in front turns four operator tests red |
| Human-gated outbound workbench | VERIFIED LOCAL | Browser-verified lifecycle on an isolated store: source + observed fact + economic hypothesis + low-friction CTA → persisted teardown/draft → approval → operator-confirmed manual send → meaningful reply. Metrics moved to 1 delivered / 1 reply on `email/named_buyer`; suppression is checked at creation and again before recording send; no autosend exists |
| Signal-led buyer intelligence | VERIFIED LOCAL | Durable intent-event and identity registries feed a non-compensatory fit/freshness/confidence gate and transparent heuristic rank. The buyer desk exposes observed fact, inferred commercial meaning, unknown identity, score components, recommended human action and Signal → Draft → Send/Reply lineage. A public-page enrichment provider is bounded to public HTML, rejects loopback/private targets and labels candidates `observed_published` rather than verified. Browser lifecycle used an isolated store; the real store has zero fabricated signals. Evidence: `EV-GROWTHOPS-BUYER-DESK-01` |
| Public hiring signal connector | VERIFIED LOCAL | Operator-supplied public careers pages are fetched through the bounded public-source client. Structured `JobPosting` records and explicit commercial vacancy links become preview candidates; non-commercial, stale, expired and future postings are rejected. Review/import is human-gated, exact duplicate signals are blocked, and every hypothesis states that a vacancy does not establish budget, vendor demand or buying intent. Live browser evidence: one current Airwallex revenue-operations vacancy → one reviewed signal → one eligible account; rescan offered no duplicate; zero drafts or outreach. Evidence: `EV-GROWTHOPS-HIRING-CONNECTOR-01` |
| Companies House officer connector | DEPLOYED | The statutory register is the one place UK decision-makers are published by law, which is the gap `EXP-ACQ-0002` measured at **0 reachable named buyers across 18 accounts**. A public officers page is read through the same bounded public-source client and yields a **named person, statutory role, appointment date and the register's own stable officer id** — an identity, never an address. Resigned officers, secretaries, corporate officers, future-dated and out-of-window appointments are rejected rather than padded in, and every candidate records `strength 2` against `confidence 0.95`: the filing is certain, the buying intent is not, and the interpretation says the register publishes the office and not the commercial remit. Reuses the hiring source/candidate store, sweep, dedupe, minimum-interval guard and human review path unchanged — an appointment is a hiring event — and dispatches on the source host, so `age source-add` and the existing daily cron pick it up with **no new command and no schedule change**. Evidence: 19 new tests, full suite 264 → 283 with the original 264 still green; parsed against two live register pages, where a page reading `2 officers / 1 resignation` returned exactly 1 candidate and `P-47` resolved to `P-47`. Mutation-checked from a proven-green baseline: resigned guard `KILLED`, recency boundary `KILLED`, both restored byte-identical. **Not yet VERIFIED LOCAL**: no candidate has been reviewed and no signal recorded in the real store, so no officer has entered the queue and no contact has been made. Known ceiling: the sweep and its cron pass their own 45-day window; register sources want `--max-age-days 90` |
| Register identity supply | VERIFIED | **96 named directors across 34 of 51 accounts**, 34/34 register pages fetched, 0 failures, mean 2.8 per company. Resolution accepted only where exactly one live search result carried the prospect's name after stripping legal form; 0 ambiguous, 17 unresolved and left unresolved. Tightening the rule to legal form alone **raised** clean resolution from 34/5-ambiguous to 34/0 and removed two guesses — *Slink* had matched `SLINK GROUP LIMITED`, a separate legal entity. As a recency signal the answer is **0 at 90 days** (2 at 180, 8 at 365, 12 at 730): `age sweep-sources --max-age-days 90` returned 0 candidates and recorded 0 signals, and the window was not widened to produce a number. Positive control against reading 0 as a broken parser: the same pages, same parser, unbounded window, 96 directors from 34/34 companies. This does **not** reopen the email route — `DISCOVERY-RESULT.md` stands, no address is published and none was inferred — it supplies the names `EXP-ACQ-0003` had no evidence existed. Names are not committed: `.age/` is gitignored and this repository is public. Evidence: `experiments/EXP-ACQ-0002/REGISTER-IDENTITY-RESULT.md` |
| `EXP-ACQ-0003` identity verification | VERIFIED | **39 of 51 accounts carry a LinkedIn identity (76.5%); 48 identities persisted** — 26 two-source (register **and** profile agree, confidence 0.8), 13 single-source (profile only, 0.6). All `observed_published`, never `verified`: a page was read, a person was not contacted. Requiring two sources corrected the search on its own terms twice — ADL Consulting was heading for a **New Zealand** firm and Technical Drive for someone who is not a director — and removed rival entities for Wavex, HBP, TWC, BCN and Blue Frontier. Where the register **disagreed** (Chorus: search says P-16 MD, register says P-36) the record stays single-source rather than being promoted. Method ceiling: LinkedIn rate-limits automated profile fetching at ~12 requests (HTTP 999); it was not evaded and no logged-in session was used, so most records rest on a search-engine rendering of the profile title rather than the page itself. The 12 with no identity split into too-new-or-small (cyberISMS, Fortitude Cyber, Aursec — all incorporated 2024–25) and name-collision (TWC, Foresight, DSI, System Force). **Effect on the contract:** the minimum sample falls to 39 under the clause already written into it; `invite_accept_rate` still measures fine at n=39, but reaching 29 accepts would need a **74.4%** accept rate (111.5% on two-source alone), so the PROCEED band is unreachable on this supply and the reply rate cannot become decisive. That is the ADJUST reading: expand the list, do not buy a subscription — a subscription fixes the invitation, and the invitation is not what is short. Ownership changes noted for re-qualification: Bluecube (Ekco), totality services (Lyra), Zenzero (Macquarie), M247 UK arm (Convergence). Zero invitations sent. Evidence: `experiments/EXP-ACQ-0003/IDENTITY-VERIFICATION.md` |
| `EXP-ACQ-0003` account expansion | DEPLOYED | **40 sourced candidates, none qualified and none seeded.** Pool: Companies House advanced search, SIC 62020/62090, active, incorporated <= 2019 — recorded as a **SAMPLING HEURISTIC**, not a market finding: three 2024–25 companies failing identity verification is an observation, and "recent incorporations are poor prospects" remains an unvalidated inference; name containing cyber / it support / managed it; 316 after de-duplication against the 63 on file. A guessed domain is a hypothesis and the fetched page is the evidence: a candidate keeps a website only where the live page's own title names the company, then the homepage must show an acquisition route **and** managed-service language. **40 survived (12.7%)**, 39 carrying a current director, 64 named directors at mean **1.6** per company against 2.8 across the existing 51 — these are materially smaller firms, 23 of 39 having exactly one director. That cuts both ways and the reviewer decides: the founder is the buyer and there is no gatekeeper, but a one-director firm may not have the acquisition function the £1,500 offer assumes is failing. Reject the single-director firms and the usable expansion is **16**, not 40. Deliberately **not** seeded: `scoreboard.qualified_prospects` counts every status that is not `disqualified%`, so seeding would have moved a VERIFIED number from 51 to 91 by adding accounts nobody qualified. Scoreboard still reports 51. Pool depth remaining: `cyber`+62020 alone holds 858; this run read six pages per combination. Two bugs found and fixed by checking a suspicious zero — the results parser was swallowing a screen-reader span, making every domain guess `<name>linkopensanewwindow.co.uk` and returning 0 of 246; and the date filter used `incorporatedTo` where the form takes `incorporationToYear`. Evidence: `experiments/EXP-ACQ-0003/ACCOUNT-EXPANSION.md`, `candidates.csv` |
| Qualification semantics | VERIFIED FIXED | `scoreboard.qualified_prospects` read `NOT LIKE 'disqualified%'`, making qualification the **default** and every other state qualified by omission. It was silently counting 10 rows nobody qualified — 5 `research`, 5 `ready_for_deep_research` — reporting **51 where the qualified figure was 41**. Now `LIKE 'qualified%'`, with `unreviewed_prospects` reported beside it so the queue stays visible rather than the inflation. Five regression tests, including one asserting that seeding 40 sourced candidates moves the qualified count by exactly zero. Mutation-checked from a green baseline: reintroducing the old predicate is `KILLED` by 4 tests, file restored byte-identical. The 10 unreviewed rows also explain why 46 accounts hold a LinkedIn identity while only 39 are qualified |
| Batch-02 qualification | VERIFIED | Protocol frozen **before** review; director count explicitly **not** a gate. **40 reviewed → 10 QUALIFIED (25.0%), 10 BORDERLINE, 20 REJECTED**; only the 10 seeded, as `qualified_batch_07`. Withdrawing the single-director rejection rule was correct: single-director firms qualify at 5/23 (22%) against multi-director 5/16 (31%), **Fisher exact two-tailed p = 0.71**, and the rule would have deleted **half of every qualified account**. `ownership_structure` retained as an observational field for later comparison at acceptance and reply. Identity resolution among QUALIFIED: **7/10 (70%)**, two-source only. Evidence: `BATCH-02-QUALIFICATION-PROTOCOL.md`, `BATCH-02-QUALIFICATION-RESULT.md` |
| Acquisition funnel | DEPLOYED | `sourcing_runs` table plus `record_sourcing_run` / `sourcing_funnel` and `age sourcing-funnel`. Rates are stored **per run**, because dividing a batch's qualified count by the portfolio's evidenced count invents a denominator — the same defect `funnel_rates` guards one stage lower. BATCH-02: 40/316 = 12.7% → 10/40 = 25.0% → 7/10 = 70.0%, compound **2.215%**. A zero denominator returns `rate: None`, never 0.0; mutation-checked `KILLED` from a green baseline. Writes are refused when qualified+borderline+rejected does not account for every evidenced candidate, or when identity exceeds qualified. **Supply: 39 qualified accounts hold an identity against a 100 target; closing the 61 gap needs ~2,754 raw register companies (CI 1,014–11,485) against a reachable pool of ~1,411 — so reaching 100 requires WIDENING the sourcing, not deeper paging.** Not extended: that is a decision, not taken here |
| `EXP-ACQ-0003` execution cohort | DEPLOYED | **39 accounts frozen, zero invitations sent.** Admission requires an explicit `qualified%` status **and** a LinkedIn identity: 28 two-source, 11 single-source, 26 MULTI_DIRECTOR / 5 SINGLE_DIRECTOR / 8 UNKNOWN, and **7 identities excluded** for sitting on never-qualified rows — the same defect as the scoreboard bug, one stage down. The invariant *no downstream state without admissible upstream lineage* is now enforced rather than described: `freeze_execution_cohort` admits only qualified accounts (mutation-checked — relaxing it back to `NOT LIKE 'disqualified%'` is `KILLED` from a green baseline, restored byte-identical), `record_invitation` refuses any prospect outside the cohort and refuses a changed treatment, and **a test asserts 39 accepted connections move `meaningful_responses`, `discovery_calls`, `commercial_proposals`, `paying_customers`, `collected_revenue_pence` and `outreach_sent` by exactly zero** — acceptance is access, never demand. Bands are counts asserted at every boundary (0/7/8/28/29/39): 0–7 PIVOT, 8–28 ADJUST, 29–39 PROCEED; verdict is `NOT_EVALUABLE` until all 39 settle, and ADJUST computes expansion from the observed rate (`ceil((29−accepted)/rate)` — 56 at 12 accepts, not 100). Accept rate divides by invitations **submitted**, never cohort size. **The send itself is an operator action and is stopped at the boundary**: it needs a signed-in LinkedIn account, the preregistration forbids using the founder's session or automating around platform limits, and each invitation is external contact with a named person. Send sheet ready at `.age/exp-acq-0003-send-sheet.md`, deliberately uncommitted — public repo, personal data. 311 tests. Evidence: `experiments/EXP-ACQ-0003/EXECUTION-COHORT.md` |
| Growth Command Center | VERIFIED LOCAL | Interactive operations console connected to 51 real actionable prospect rows. Buyer desk, intake, workbench and reporting surfaces execute real persisted workflows; state mutations require an explicit intent header and the loopback-only server remains no-store/CSP/frame protected. Evidence: `EV-GROWTHOPS-COMMAND-CENTER-01` |
| Revenue Intelligence workspace | VERIFIED LOCAL | `/revenue-intelligence` is an operating record, not a dashboard: machine-observed data is set in monospace and human prose in sans, and colour is epistemic only — green observed, olive inferred or unknown, sienna held by a gate. An observation earns a ruled provenance line carrying its date, a measured confidence tick and its source; an inference gets no rule. Queue, held-by-a-gate and saved-source ledgers plus a buyer dossier with signal history, first-class unknowns and a commercial chain whose unobserved stages read `not observed yet`. The prospect holding the store's strongest signal and its only verified identity stays out of the queue on a `disqualified` status. A forced 503 renders an error, not an empty dataset. 0 px horizontal overflow at 1440×1000 and 390×844, dossier open and closed; keyboard and focus-return verified. Review: `marketing/revenue-intelligence-visual-review-2026-08-30.md` |
| First ranked buyer | VERIFIED LOCAL | The queue held its first entry on 2026-08-30. Three swept candidates were reviewed against their live sources and **two were rejected**: Blue Frontier's was site navigation labelled with the company's own service line, linking back to the careers page itself; The HBP Group's was an evergreen talent pool (`/jobs/sales-careers`, 'Are you a sales professional…'), one of a family with `/jobs/it-careers` and `/jobs/future-opportunities`, while every actual HBP vacancy is technical. Both dismissals record their reason — a dismissal without one is a deletion, and the code now refuses it. Atlas Cloud's `Customer Success Manager (CSM)` verified as a real vacancy at its own URL under Department: Sales, with revenue-retention and cross-sell targets, and was recorded as `SIG-349ABEAEF68B`. Identity resolved to the published CEO LinkedIn profile — a declared ICP role — and labelled `observed_published`, not verified; the two role inboxes the provider also returned were deliberately not recorded. Queue: 1 eligible, priority 53.2, route linkedin, action `prepare_approval_draft`, authority R3. **No contact has been made** |
| Careers source discovery | VERIFIED LOCAL | `age discover-sources` reads each prospect's home page, finds careers links by path **and** by link text, and keeps a source only where a commercial vacancy is actually published. Every prospect lands in exactly one of four distinct outcomes — published / no commercial role / no careers link / site unreachable — because an unreachable host recorded as `not hiring` is a market answer that was never given; collapsing `site_unreachable` into `no_commercial_role` turns that test red. Run across all 51 non-disqualified prospects on 2026-08-30: **3 published a commercial role** (Atlas Cloud, Blue Frontier, The HBP Group), 29 had a readable careers page with none, 16 exposed no careers link, 3 were unreachable. The 3 are seeded in `seeds/registries.json` and swept into 3 candidates awaiting human review. **The queue is still 0** — a candidate is not a signal |
| Scheduled signal source sweep | VERIFIED LOCAL | Public careers pages are saved against a prospect, seeded durably in `seeds/registries.json`, and swept by `age sweep-sources` — installed as a daily 07:00 cron entry via `scripts/sweep-cron.sh`. A sweep records nothing as a signal: candidates persist as pending proposals a human reviews. Two guards make unattended running safe and both are mutation-verified: `--min-interval-hours 20` skips a source fetched recently, so a crash-looping schedule cannot become a crawl; and a conflict refreshes only `last_seen_at`, so a candidate a person dismissed is never resurrected by a machine. Verified live: the exact cron command runs under `env -i`; three real UK MSP careers pages swept clean, then all three correctly skipped on the immediate re-run. Install/remove/reinstall proved idempotent with 9 other-project cron entries intact. Evidence: `.age/cron/sweep.log` |
| Commercial performance surface | VERIFIED LOCAL | Conversion at each funnel step is computed against the step above it and surfaced next to the intelligence meant to move it. A rate needs a denominator: 0 replies from 12 sends renders as a measured `0.0%` on a solid track, 0 from 0 renders as `NOT ASKED` on a dashed one, and the two are never shown the same way — collapsing them is how a delivery failure gets read as market rejection. CAC, LTV and payback are named as `not derivable` with the reason, because nothing records acquisition spend, monthly revenue or churn yet. Mutation check: making a zero denominator report 0.0 turns four funnel tests red. Verified at 1440×1000 and 390×844 against a fixture carrying all three states at once |
| Universal experiment contract | DEPLOYED | One persisted contract covers all 13 experiment namespaces, evidence lineage, controls, metrics, economics, samples, budgets, thresholds, dates, decisions and learning |
| Social conversion and attribution contract | DEPLOYED | Content, profile, DM, contact capture, offer, customer and revenue IDs share one lineage; deterministic tests cover funnel rates, audience capture, legacy schema migration and unknown denominators |
| Unit and lifecycle economics decision layer | DEPLOYED | Integer-pence models distinguish CAC from contribution profit and add realised 30/90/365-day value, gross profit per acquired customer and expansion rate; deterministic tests cover edge cases |
| Revenue-evidence OS `v0.1.0` | VERIFIED | Public [release](https://github.com/Thepluscode/ai-growth-engineering/releases/tag/v0.1.0) at commit `2af2070`; remote [test workflow](https://github.com/Thepluscode/ai-growth-engineering/actions/runs/32263253558) passed |
| `EXP-ACQ-0001` preregistration | VERIFIED | 50-send minimum; KEEP at >=10%, ITERATE at 5-9.9%, REVIEW below 5%; contract held to the end without amendment |
| `EXP-ACQ-0001` verdict | VERIFIED | `REVIEW` at n=50, observed 0.0, recorded 2026-08-27 by `age experiment-result`; written up in `experiments/EXP-ACQ-0001/VERDICT.md` |
| 50 qualified prospects | VERIFIED | 57 records: seven are explicitly disqualified; Batches 04 and 05 add 20 current UK accounts; CLI reports 50 and the reseed regression test prevents stale qualification statuses from inflating the count |
| Queue-01 deep research | VERIFIED | 10 queue accounts researched from cited public sources; Foresite disqualified; Morcan researched as send replacement |
| Queue-01 teardowns | VERIFIED | 10 queue decisions plus Morcan replacement under `experiments/EXP-ACQ-0001/sales/teardowns/`; observed/inferred/unknown separated |
| Queue-01 messages | VERIFIED | 10 qualified messages sent through Gmail on 2026-08-19; full copy in `experiments/EXP-ACQ-0001/sales/outbound-messages.md` |
| Queue-02 qualification | VERIFIED | 10 next-batch accounts qualified in `experiments/EXP-ACQ-0001/sales/batch-02-qualification.md`; RSK and Stingrai disqualified during qualification, then standalone QuoStar was replaced by current owner Zenzero after its live redirect was observed |
| Queue-02 teardowns | VERIFIED | 10 evidence-backed teardowns in `experiments/EXP-ACQ-0001/sales/teardowns/12-cyberisms.md` through `21-chorus.md`; every file passes the ten-section contract and separates observation from inference |
| Queue-02 messages | IN PROGRESS | 9 of 10 messages confirmed SENT; Slink remains unsent because its live site exposes only a contact form and browser submission requires action-time confirmation |
| Queue-03 qualification | VERIFIED | 10 current accounts qualified; Bulletproof and Bridewell disqualified during verification and replaced with clean wedge fits |
| Queue-03 teardowns | VERIFIED | 10 evidence-backed teardowns in `experiments/EXP-ACQ-0001/sales/teardowns/22-air-it.md` through `31-mitigo.md`; 100 required sections validated |
| Queue-03 messages | DEPLOYED | 10 personalised OBSERVATION + ECONOMIC HYPOTHESIS + LOW-FRICTION CTA messages staged and verified as Gmail drafts; none counted as sent |
| Queue-04 qualification and teardowns | VERIFIED | 10 current accounts qualified and 10 evidence-backed teardowns completed; all 100 required sections and inference labels validated |
| Queue-04 messages | DEPLOYED | 10 personalised messages staged and verified as Gmail drafts after duplicate and suppression checks; none counted as sent |
| Queue-05 qualification and teardowns | VERIFIED | 10 current accounts qualified; Six Degrees disqualified as an enterprise-scale outlier and replaced with Inology; all 100 required teardown sections and inference labels validated |
| Queue-05 messages | DEPLOYED | 10 personalised messages staged and verified as Gmail drafts after duplicate and suppression checks; none counted as sent |
| Qualified outbound sends | VERIFIED | 50 counted sends from 55 rows; `age import-outreach` then `age scoreboard` reports `outreach_sent 50/100`, five bounces excluded |
| Send recipient composition | VERIFIED | 48 of 50 went to a role inbox; 2 reached a named buyer's own address. The route actually tested was shared-inbox email, not buyer email |
| Queue-01 follow-up plan | DEPLOYED | Manual, value-adding first follow-ups prepared in `experiments/EXP-ACQ-0001/sales/follow-up-plan.md`; do not send before reply and suppression review on 2026-08-24 |
| Meaningful replies | VERIFIED | 0 of 50. Wilson 95% CI 0-7.1%; P(0 replies at a true 10%) = 0.52%, at a true 5% = 7.7%. Texaport's intake ticket remains automation, not a conversation |
| Named-buyer route | PLANNED | n=2, P(0 replies at a true 10%) = 81%. Untested, and must not be reported as failed |
| `recipient_class` on every send | VERIFIED | Required validated column; unknown values raise instead of defaulting; legacy rows report `unclassified` rather than joining a route. `age recipient-split` reports named_buyer 2/0 and role_inbox 48/0. Mutation-checked from a green baseline: `KILLED`, precise, file restored byte-identical |
| `EXP-ACQ-0002` preregistration | DEPLOYED | Named-buyer route only; 30-send minimum, same 10%/5% thresholds so the routes stay comparable; contract frozen in `experiments/EXP-ACQ-0002/PREREGISTRATION.md`; zero sends executed |
| `EXP-ACQ-0002` contact discovery | VERIFIED | **0 reachable named-buyer mailboxes across 18 observed accounts.** Wilson 95% CI 0-17.6%, ceiling ~10 of 55; 30 would need a 54.5% publication rate (P = 7e-7). Written up in `experiments/EXP-ACQ-0002/DISCOVERY-RESULT.md` |
| `EXP-ACQ-0002` execution | BLOCKED | Does not start. The contract stands unamended and unrun; it was not relaxed to fit the supply. Revival condition: 30 named-buyer addresses obtained without guessing |
| Email wedge for this ICP | VERIFIED CLOSED | Mid-market UK MSPs do not publish commercial decision-makers' addresses, so cold email is structurally a shared-inbox channel here — measured at 0/48. A property of the segment, not of the message |
| LinkedIn route feasibility | VERIFIED | Reachable but paid and not comparable. LinkedIn Help, first-party: free accounts get **5 personalised connection notes per month** — a 30-send minimum takes six months. Sales Navigator Core US$119.99/mo (50 InMail); Premium is likely the cheaper correct purchase. Written up in `experiments/EXP-ACQ-0003/FEASIBILITY.md` |
| LinkedIn route metric | VERIFIED | Not comparable to `EXP-ACQ-0001`: invite → accept → message → reply, and an unaccepted invite is a silent non-event, not a bounce. Thresholds must come from the invite-accept step, which has no baseline. The character-limited note also forces a message change, confounding channel with message |
| `EXP-ACQ-0003` | DEPLOYED | **Preregistered 2026-09-08 at £0 — the spend decision is deferred, not taken.** The confound `FEASIBILITY.md` demanded be designed around is removed by taking the message out of the note: a **standard invitation with no note**, then the unchanged `EXP-ACQ-0001` message as a direct message after acceptance. LinkedIn Help, first-party: *"Restrictions and limits on standard invitations are separate from the limits placed on personalized invitations"*, and a free account may message a 1st-degree connection — so the five-notes-a-month cap that made this a six-month experiment does not apply, and no subscription is needed to run it. Primary metric is **`invite_accept_rate`, not reply rate**, because the arithmetic was done before the contract was frozen rather than after: at a 51-account supply ceiling the reply stage needs 29 accepts to reject KEEP (a 56.9% accept rate) and 59 to reject ITERATE (impossible at any accept rate). Preregistering reply rate would have repeated `EXP-ACQ-0002`'s exact defect. Decision bands are derived, not chosen: ≥56.9% PROCEED · 19.3–56.8% ADJUST, expand the list, a subscription fixes the wrong step · <19.3% PIVOT, the invitation is the constraint and a paid personalised note becomes `EXP-ACQ-0004`. Gojiberry AI at $99/mo sits on that same PIVOT branch and is not preregistered here. Contract persisted via `age experiment-add`, `budget_pence 0`. Zero invitations sent, nothing bought. Evidence: `experiments/EXP-ACQ-0003/PREREGISTRATION.md` |
| `EXP-ACQ-0007` | DEPLOYED | **Preregistered 2026-09-16. Cohort frozen, draw recorded, 0 messages generated, 0 sent.** One variable: the message-generation procedure. `baseline` = `theplus.outreach-research@1.0.0` (`11ab2c98…`), `candidate` = the admitted `marketingskills_cold_email@2.0.0` (`5736b9db…`), both frozen in `experiment_procedures` as VARIABLE at 2026-09-16T12:03:34+00:00. 60 qualified uncontacted UK MSP/cyber/IT-services accounts, 30/30 by a seeded SHA-256 keyed sort (seed `EXP-ACQ-0007/2026-09-16`, input `8cc4bf9b…`, assignment `6ccd4dfc…`) — one draw, no rerandomisation, balance reported and never optimised against. Primary metric `qualified_reply_rate`, AGE's existing controlled-effect metric, not a new one. **The arithmetic was done before freezing:** at 30 per arm the candidate needs 10/30 against a 3/30 baseline to clear z 1.96, the minimum detectable difference at a 10% baseline is 21.7% against a 10% policy line, and each side has exactly 30 accounts for a 30-exposure floor — so a null returns INSUFFICIENT_SAMPLE, never NO_DIFFERENCE, and one bounce can take a side below the denominator. Recorded as a known limit, not repaired by substitution. Two caution-flagged accounts kept and recorded as covariates; both drew into `baseline`. Evidence: `experiments/EXP-ACQ-0007/PREREGISTRATION.md` |
| `EXP-ACQ-0007` reserve + message review | DEPLOYED | **Pre-treatment amendment, `OUTREACH_SENT = 0`.** 30 per arm against a 30-matured-exposure floor left no tolerance for an invalid account, so a **13-account reserve pool** was sourced under the same frozen rule (0 overlap with the 60 or the prior-contact list) and a replacement rule frozen before generation: rank = `sha256("EXP-ACQ-0007/reserve|" + company_number)`, lowest unused rank first, **replacement inherits the invalidated account's arm**, no rerandomisation. A reserve may repair an account never validly exposed and may never repair a result. AGE policy already agrees on bounces — `DELIVERED` excludes `message_bounced`, so a hard bounce never entered the denominator; no policy was changed to fit. `PREREGISTRATION.md` untouched at `c726c736…`; amendment `da0c1e44…`, reserve `fb007ed5…`. **Message review:** 6 messages, 3 accounts, same evidence both arms — control mean 27.00 vs treatment 26.33, all SEND-eligible, but the treatment trades evidence (5→3) for lower-friction asks (3→5), drops the source citation in 3 of 3, and asserts prospect state twice on one account. Suppression check recorded as **vacuous** (0 rows in the table). Evidence: `experiments/EXP-ACQ-0007/AMENDMENT-01-RESERVE-POOL.md`, `MESSAGE-REVIEW.md` |
| `outreach.channel` | VERIFIED | A LinkedIn send can no longer be counted in the email numbers. `age recipient-split` keys on `channel/recipient_class`; a row with no channel reports `unknown`, never `email`. Mutation-checked from a green baseline: `KILLED` precise on the grouping, sentinel fired on the channel default; both restored byte-identical |
| Discovery calls | PLANNED | 0; use `experiments/EXP-ACQ-0001/sales/discovery-checklist.md` |
| First proposal | PLANNED | Sell Diagnostic or Sprint only when discovery economics support it |
| First payment / customer dataset (`v0.2`) | PLANNED | No evidence yet |
| Commercial `v0.2` gate | IN PROGRESS | Architecture slice is implemented; commercial evidence is 50 counted sends, 0 meaningful replies, 0 discovery calls, 0 revenue. `age gate-check` reports REVENUE GATE: NOT MET |
| Owned-site baseline window | VERIFIED CLOSED | Closed early 2026-08-28. Read from Vercel's API, not the dashboard: **11 visitors / 14 pageviews in the clean 7-day window** (~1.6/day, ~47/month). All 35 all-time visitors have an empty referrer — no channel is pointing at the site |
| `EV-BASELINE-01` | WITHDRAWN | "47 sessions, 3 events, 6.4%" cannot be reproduced. Cumulative as of 2026-08-21 was 24 visitors / 36 pageviews; no day reached 47 of anything, and it is not an environment-filter artefact. The conclusion drawn from it was wrong by ~30x and overturned a correct prediction |
| Conversion numerator | BLOCKED | `contact_intent` is a custom event; `events/count` returns **402 — requires Pro or Enterprise** and the team is on `hobby`. The numerator is unreadable now and in future without an upgrade |
| `EXP-CREATIVE-0001` | NOT RUNNABLE | Constraint is audience, not creative. At 11 visitors/week the most generous power cell (870 sessions, 5%→10%) takes **1.5 years**; 1%→1.5% takes 27 years. Nothing preregistered |
| Distribution | VERIFIED CONSTRAINT | Three independent measurements agree: shared-inbox email 0/48, personal email route unreachable, owned site ~47 visitors/month with zero referrers. Nothing in creative, offer or messaging has been falsified — none of it has been given a sample |
| Channel registry accuracy | VERIFIED | Six of eight rows corrected against measurement in `seeds/registries.json`. `CH-OWNED-SITE` and `CH-PAID` both carried conclusions derived from the withdrawn 2026-08-20 reading |
| Untried channel screen | VERIFIED | `channels/CHANNEL-SCREEN.md`, criteria fixed before assessment. Criterion 3 reorders everything: visitor counts are free, conversions are 402-paywalled, so any channel judged by an on-site conversion is unmeasurable today |
| Organic search | UNMEASURABLE | `robots.txt` and `sitemap.xml` serve 200 and the sitemap is well-formed, but two exact-phrase searches returned nothing from the domain and **Google Search Console is not connected**. Without GSC the channel has no denominator. Sitemap `lastmod` stale at 2026-08-13 |
| LinkedIn organic | MEASUREMENT OPEN | Window defined in `channels/social/MEASUREMENT-WINDOW.md`: no `EXP-` id, no thresholds, minimum **8 posts**. Primary reading is **viewer job titles and companies**, not impressions — a rising number with the ICP absent is the named false positive. Unit is the post; median reported with the total. All metrics free; neither open spend decision blocks it |
| Google Search Console | ONE ACTION LEFT | A `google-site-verification` TXT **already exists** beside a Workspace SPF record, so a Domain property under the same Google account should verify immediately — no DNS or site change. DNS is on Cloudflare, not Vercel. Steps in `channels/seo/GSC-SETUP.md` |
| Website redesign | VERIFIED SHIPPED | `redesign/v2` merged and deployed 2026-08-28; production is `main@86654c5`. Verified against the live origin: `/workflow-audit` and its sample report now **200** after returning **404** in production — the only pages describing the £1,500 offer. `/nope` 404s. Homepage 118,170 → 20,145 bytes |
| Analytics survived the merge | VERIFIED | `tpt_internal_traffic` and `contact_intent` both still present in the served bundle after the redesign landed |
| Favicon defect | VERIFIED FIXED | Shipped **served but unreferenced** — asset 200, no `rel="icon"` in the HTML, browsers falling back to a 404ing `/favicon.ico`. Found by grepping the deployed page, not by reading the commit message |
| Site visual review | VERIFIED | Done in a real browser at 1440 and 390 on 2026-08-29. 0px overflow both widths, all routes 200, `/nope` 404, one console error and it was my own probe. `marketing/visual-review-2026-08-29.md` |
| Internal-traffic exclusion | VERIFIED END TO END | First real-browser test: `?internal=1` set `tpt_internal_traffic="1"` and logged `internal traffic exclusion: ON`. The review used it, so it did not contaminate the measurement |
| Offer congruence | VERIFIED FIXED | Was **16/30 DO_NOT_SPEND** — two products at one price, zero shared deliverables. Now **29/30 ELIGIBLE_FOR_TEST**: 5/5 deliverables shared, verified on both live pages, old labels 0 occurrences, title corrected. Root cause was a duplicated offer definition, not positioning — `PRODUCT.md` had marked the framing binding all along |
| Offer registry gap | VERIFIED FIXED | The registry did not contain the company's own flagship. `OFF-RESCUE-SPRINT` added — £1,500, 0 sends, 0 buyers |
| Offer strategy conflict | **OPEN — founder decision** | Two problems marketed to the same buyer. `EXP-ACQ-0001`'s 50 sends pitched a **growth-leak teardown** ("acquisition not becoming qualified pipeline"); the site sells **questionnaire rescue** ("trust answers rebuilt every deal"). Neither has a buyer. Not resolved: the outbound offer has measured non-response (0/48), the site offer has never been taken to market, so no evidence separates them |
| Contact address | FLAGGED | Every CTA on both pages is `mailto:<owner-contact>`, and the offer page says "**Confidential** enquiries to" it. The domain already runs Google Workspace, so a domain mailbox is one console action away |
| Separate-repository import | VERIFIED LOCAL | On 2026-09-03 a session started `~/projects/ai-growth-engineering` instead of working here; work continued there until the founder moved it back on 2026-09-15. Full copy at `imports/marketing-engineering-os/` (git-ignored — personal data, public repository): 1,104 files, byte-identical checksums against the source, own virtual environment, its CLI reads live FS-001 state from the copy. The source directory was deleted on the founder's instruction on 2026-09-15, after a final comparison found 0 files that existed only there (6 stray git commit objects were copied first) |
| `EXP-ACQ-0005` (was `EXP-0003`) | HELD | CTA size on named buyers, AI-agent vendor ICP. TOPUP-002 Apollo lookup ran (65 frozen identities → 2 verified on-domain, 1 unavailable, 62 unmatched) and was imported: **16 message-ready control / 18 variant**, 0 incidents, 0 prior contact on the two new domains. Far below 29 per arm — descriptive only. Held so `EXP-ACQ-0003` goes to market first. Zero sends. Evidence: `experiments/EXP-ACQ-0005/README.md` |
| `EXP-ACQ-0006` (was `FS-001`) | LIVE | UK field-service dispatch sprint. 22 Stage A routing emails sent 2026-09-08, 1 bounce, 21 awaiting outcome; **no verdict before 2026-09-22**. Record outcomes from the imported copy. Evidence: `experiments/EXP-ACQ-0006/README.md` |
| Marketing Engineer revenue loop — first slice | VERIFIED LOCAL · LOOP BLOCKED ON REVENUE | Extends the existing store; architecture in `docs/MARKETING_ENGINEER_ARCHITECTURE.md`. **Built:** an append-only `funnel_events` log (UPDATE/DELETE refused by trigger; corrections appended, never rewritten; synthetic fixtures excluded from every total), adapters from the real send log and the invitation cohort, 19 metric definitions stored beside their values (a zero denominator returns None, and so does unrecorded spend), first/last/linear attribution in whole pence, a money graph for a buyer or a campaign, a rule-based evaluator, `recommend_next_experiment()`, a single declared variable on the experiment contract, and `age marketing-engineer status`. **Real data run (local store):** EXP-ACQ-0001 log → 50 `message_sent` + 5 `message_bounced` (matches the scoreboard's 50), EXP-ACQ-0003 → 7 `invitation_sent`; re-import appends 0. The evaluator reads EXP-ACQ-0001 as a measured zero (P = 0.52% at a true 10%) that tested access rather than the message (48 role inbox / 2 named buyer), and EXP-ACQ-0003 as unmeasured (7 of 51). It recommends running the frozen EXP-ACQ-0003, with EXP-ACQ-0002 blocked for no supply. 341 tests; 10 guards mutation-checked, all detected and restored. **Blocked:** no real reply, meeting, proposal, customer, payment or spend event exists, so attribution, CAC and ROAS have nothing to act on. Not built, by decision: paid-platform adapters, creative fatigue, retargeting/lookalike, LLM interpretation |
| Commercial funnel diagnosis (P1 + P2) | VERIFIED LOCAL | `diagnose_funnel()` + `age marketing-engineer diagnose`. Stage ladders from delivery to payment (invitation / outbound / paid); a buyer counts at every stage up to the furthest one reached, and a downstream event with no exposure is reported unlinked, never counted. Undeliverable invitations become `invitation_undeliverable` (an attempt, not exposure; earlier `invitation_sent` rows are corrected). The first response step counts only exposures older than a 14-day window, so a fresh invitation is IN_FLIGHT, not declined. Per transition: NOT_REACHED / IN_FLIGHT / INSUFFICIENT_DATA / MEASURED with rate and Wilson CI; the largest *measurable* constraint with confidence LOW/MEDIUM/HIGH, called a leak only when MEASURED. `experiment-execution-mode` records DESCRIPTIVE_FROZEN_COHORT without touching the contract, so the 51 minimum is no longer claimed. **Real data (2026-09-15; first published mislabelled 2026-09-16):** EXP-ACQ-0001 0/50 replies, MEASURED, MEDIUM, 5 bounces excluded; EXP-ACQ-0003 34 delivered (4 undeliverable corrected out), accept step IN_FLIGHT, verdict DESCRIPTIVE_INCONCLUSIVE. 346 tests; 3 new guards mutation-killed (window, measured-first ranking, undeliverable) |
| Campaign / creative / audience model, money graph and next-experiment card (P4 + P5 + P6) | VERIFIED LOCAL · NO REVENUE TO TRACE | Two generated registries (`campaigns`, `audiences`) plus `campaign_id` / `body` / `version` on `creatives`. Campaign status and audience type (cold / retargeting / lookalike / customer) are closed sets. Events with no campaign inherit one only when their experiment has exactly one registered campaign, and every inherited row is marked. `campaign_graph()` joins offer, audience and creatives to counts that each list their event ids, plus spend, pipeline, customers, attributed revenue, CAC, ROAS and pipeline ROAS (not derivable when spend is unrecorded). `customer_graph()` traces a buyer back to ICP, offer, campaign, creative, experiment and channel. `next_experiment_card()` / `age marketing-engineer next-experiment` render the ONE preferred test in decision form: constraint, evidence, uncertainty, variable / control / variant, metric, keep-constant (from records; the variable under test is released; an unpriced offer shows "not recorded", never £0), kill condition. Real rows seeded in `seeds/registries.json` for EXP-ACQ-0001 and EXP-ACQ-0003. **Real data (2026-09-15; first published mislabelled 2026-09-16):** CMP-ACQ-0003 → 34 invitations traced (38 events linked by experiment, including 4 undeliverable), OFF-TEARDOWN, cold audience, spend/CAC/ROAS not derivable; the card prefers EXP-ACQ-0003 with its constraint "none measurable yet — Delivered → Accepted IN_FLIGHT". **Resolved 2026-09-15:** EXP-ACQ-0003 `variable = recipient_route`, recorded as retrospective metadata only |
| Money ingestion: adapter contract, six mapping specs, Stripe reference adapter | VERIFIED LOCAL · NO REAL PAYMENT YET | `adapters.py`: one `MarketingAdapter` contract and `ingest()`. A rejected record is reported with its reason; an event outside the adapter's implemented mappings stops the run. Mapping specs for Meta Ads, Google Ads, LinkedIn Ads, GA4, Stripe and HighLevel give, per external record: canonical event, identity fields, provenance, idempotency key, value semantics, and what is deliberately not mapped (`age adapter-specs`). Only Stripe is implemented (`age events-import-stripe`): `payment_intent.succeeded` → `payment_received`, keyed on the payment intent so redelivery collapses. `charge.succeeded` and `invoice.paid` are refused as duplicate views of the same money. Test mode → `synthetic_fixture` (never counts). Non-two-decimal currencies refused. Conflicting company metadata leaves the buyer unknown and unattributed. A paying buyer now counts as a customer in the campaign money graph. **Demo (scratch copy of the store, fixture payment, not evidence):** £1,500 → one frozen EXP-ACQ-0003 buyer → CMP-ACQ-0003 and EXP-ACQ-0001 campaigns (that buyer was reached by both) → OFF-TEARDOWN → both creatives → £750 linear to CMP-ACQ-0003, 1 customer, spend/CAC/ROAS NOT_DERIVABLE. Real store: 0 payments. Also shipped: `experiment-backfill-variable` (EXP-ACQ-0003 `recipient_route`, retrospective; fingerprints of contract, cohort, invitations and events unchanged) and append-only invitation date correction (31 events re-dated). Not built by decision: the other five implementations, creative fatigue, retargeting, lookalikes |
| Buyer truth: evidence → buying reason → revenue | VERIFIED LOCAL (synthetic) · NO REAL BUYER EVIDENCE YET | **Durability:** `seeds/registries.json` now holds the EXP-ACQ-0003 contract, including `variable = recipient_route` (retrospective_from_preregistration, with its note) and `DESCRIPTIVE_FROZEN_COHORT`. `seed-registries` restores it on rebuild and fills only missing metadata. **Model:** the observation is an ordinary `evidence` row. `commercial_evidence` (append-only) links it to buyer, company, funnel event, experiment, campaign and offer. `evidence_interpretations` (append-only, newest wins) holds themes. A trigger refuses rewriting a linked observation. 14 categories, WTP as a six-rung ladder (payment > signed action > proposal requested > budget > stated WTP > interest). Refused: acknowledgements under 3 words, unsourced statements, an invitation/send as the source event, buyer or lineage mismatch, and REASON_FOR_PURCHASE without a purchase. Scope counted: BUYER / ORGANISATION / CROSS_ORGANISATION / SEGMENT_CANDIDATE (3+ independent organisations). `age evidence-record`, `age evidence-interpret`, `age marketing-engineer customer --company` (payment → attribution → problem / objection / criterion / reason for purchase, with evidence ids, NOT OBSERVED when absent), `age marketing-engineer problems`. Status shows BUYER TRUTH only when evidence exists. The next-experiment engine prefers an objection raised by 2+ organisations that stalled after a proposal (single variable: offer) over funnel leaks. No LLM. **Real store (2026-09-15):** 0 buyer evidence linked, 0 payments |
| Segment performance + target / offer / route decisioning | VERIFIED LOCAL (synthetic) · REAL STORE: NOT_ENOUGH_EVIDENCE | `segments.py` aggregates existing events, campaigns, offers, audiences and buyer truth. **Recorded dimensions only:** icp, buyer_role, audience_type, offer, price, campaign, experiment, channel, recipient_route, acquisition_route. Company type/size, geography and seniority are listed as not recorded, never guessed. **Units:** each (buyer, experiment) exposure is its own unit; an outcome without its own exposure goes to the buyer's most recent prior exposure. Per segment: counts from exposure to payment, observed revenue, pipeline, rates (zero denominator NOT_DERIVABLE; under 30 INSUFFICIENT_DATA; reply/acceptance only on exposures past the 14-day window), economics only through the campaign record, and buyer truth counted by organisation (a top theme needs 2+). **One `EVIDENCE_POLICY`:** NO_DATA / EARLY_SIGNAL / DESCRIPTIVE (30 matured, 10 orgs) / DECISION_WORTHY (100 matured, 30 orgs, 5 downstream); the runner-up is judged on sample only. Comparison ranks revenue > customers > accepted proposals > proposals > meetings > qualified > replies > access > delivered. It reports NOT_ENOUGH_EVIDENCE when the difference is access only or evidence is below DESCRIPTIVE, and discloses confounds (experiment, offer, ICP, audience, channel, route, non-overlapping periods, cross-experiment, overlapping buyers). DECISION requires no confounds. `recommend_target_segment()`, `recommend_offer()` (only within one ICP + audience context, else NOT_COMPARABLE), `recommend_acquisition_route()` (channel and route kept apart; access contradicted by downstream evidence is flagged). Status shows the section only after a delivery; the next-experiment engine proposes offer → route → audience from segment evidence, one variable. CLI `age marketing-engineer segments / target / offer / route`. **Real store (2026-09-15):** every recommendation NOT_ENOUGH_EVIDENCE. email/role_inbox 0/48 replies at a measured sample (DESCRIPTIVE). linkedin/named_buyer_connection 34 delivered, 0 matured (EARLY_SIGNAL). 31 buyers reached by both routes. One offer observed |
| Performance change diagnosis | VERIFIED LOCAL (synthetic) · REAL STORE: NOT ENOUGH EVIDENCE | `change_diagnosis.py`, computed on read from funnel events, the segment unit model and buyer truth. **Windows** are always explicit (`age marketing-engineer change --baseline S:E --comparison S:E`, optional experiment/campaign/arm/segment filters); the status page uses declared windows and prints them. **Maturity:** each window is MATURE / PARTIALLY_MATURE / IMMATURE from actual first-delivery dates; only matured exposures enter cohort denominators, and maturity dates are reported. **Deltas** for rates, counts, money and cost metrics give absolute (percentage points for rates) and relative change (never from a zero baseline). Meaningful only past `CHANGE_POLICY` thresholds: rates by a two-proportion test with 30+ per window; counts and money by 2+ change and 25%+. **Inputs:** composition shift (total variation ≥ 0.20) in creative, hook, body, CTA, offer, price, channel, route, audience, ICP, role, experiment and campaign, plus exposure volume and spend. Unrecorded inputs are listed. **States:** CLEAN / PARTIALLY_CONFOUNDED / HEAVILY_CONFOUNDED / NOT_COMPARABLE. **Labels:** NO_MEASURABLE_CHANGE, IMPROVED, DETERIORATED, MIX_SHIFT, POSSIBLE_CREATIVE/OFFER/ROUTE/AUDIENCE_EFFECT, MULTIPLE_CONFOUNDS, INSUFFICIENT_DATA, and CONTROLLED_EFFECT only for concurrent arms of one experiment whose declared variable is the only recorded difference. The lead change is the deepest meaningful one (revenue first); revenue changes are explained by nearby drivers, and upstream moves that did not carry through are flagged. Output gives what changed, what stayed constant, confounds, the supported interpretation, unsupported claims (including "the market changed" without 3+ organisations) and one next test. The next-experiment engine uses it after segment evidence and before funnel leaks. **Real store (2026-09-15):** declared status windows → INSUFFICIENT_DATA (no exposure 2026-08-26..09-01). EXP-ACQ-0003 09-09 vs 09-15 → IMMATURE, maturing 2026-09-23 and 2026-09-29. EXP-ACQ-0001 day 1 vs day 2 → INSUFFICIENT_DATA (5 delivered on day 2) |
| Reply capture with human approval | VERIFIED LOCAL (synthetic) · REAL: 0 BUYER REPLIES, 1 PENDING REVIEW | `reply_capture.py` + `age replies link-outbound / capture / review / approve / reject`. **Proposes, never decides.** A Gmail adapter turns retrieved threads into channel-neutral inbound messages; LinkedIn or manual adapters would produce the same shape. `outbound_messages` holds lineage only (message/thread → buyer, experiment, campaign), never a send count. `reply_candidates` holds one candidate per source message, captured once and never rewritten. `reply_decisions` holds one append-only decision per item. **Identity:** the governed thread (HIGH), a colleague at the buyer's domain in that thread (MEDIUM), or an exact sender address with exactly one buyer behind it (MEDIUM). Otherwise UNMATCHED or AMBIGUOUS; a domain alone never matches. **Excluded:** own messages, out-of-office, gateway and ticket auto-replies. Bounces map to `message_bounced` (proposed only if not already recorded); unsubscribes map to suppression. **Proposals:** `reply_received`; `reply_meaningful` only on a discussion or problem cue, never from positivity; buyer-evidence drafts from deterministic wording rules, each citing an exact clause of the new text. Quoted and repeated text is never a second observation; acknowledgements and snippets yield no evidence. **Approval:** item by item. The event comes before meaningful or evidence. Edits may change a category or narrow a citation to the buyer's own words. Writes go only through `record_event` / `record_commercial_evidence` with source gmail, the message id and the thread id. Rejected items stay recorded, so the message is never proposed again. Status shows `[PENDING REVIEW] N`; pending candidates count as nothing. **Real (2026-09-15):** 25 governed sends linked (19 EXP-ACQ-0006, 6 EXP-ACQ-0001); 12 inbound messages captured, all bounces or automated (ramsac auto-response, texaport ticket); 0 buyer replies. Positive-control search confirmed the empty EXP-ACQ-0006 result. Funnel events (165) and evidence (46) unchanged. 1 pending: the Safe House bounce on EXP-ACQ-0006, not approved |
| EXP-ACQ-0006 send / bounce lineage | VERIFIED ON REAL STORE · NO REPLY YET | **Reconciliation** (`experiments/EXP-ACQ-0006/STAGE-A-RECONCILIATION.md`): the claimed 22 are 21 VERIFIED_GMAIL_SEND and 1 UNKNOWN (FSP-0026 OEC). The first link found 19 only because the default Gmail search skips Trash (Waterworx and Manchester EICR were trashed). The empty OEC result was checked against a positive control; `UNVERIFIED_STAGE_A_SENDS = 1`. **Import:** `age replies import-sends EXP-ACQ-0006` appends one `message_sent` per linked Gmail message (source gmail, message id as idempotency anchor, real send time, thread id, campaign CMP-ACQ-0006-STAGE-A, recipient class from the address). It takes no count, refuses ambiguous recipient lineage, and never double-counts a buyer already in the funnel from another source. **Denominators:** a send followed by its bounce stays an attempt; `delivered_messages`, funnel diagnosis, segments and change diagnosis all exclude bounced (buyer, experiment) pairs. Reply-rate metrics use delivered messages. **Guard:** a reply or bounce with a verifiable governed send cannot be approved until that send is recorded, and it records `answers_send_event_id`. **Safe House:** the Gmail failure message names the governed address, 11 s after the send; `message_bounced` approved, no buyer evidence. **Real (2026-09-15):** 21 attempted, 1 bounced, 20 delivered, reply step IN_FLIGHT (matures 2026-09-22), 0 replies, 0 qualified, 0 evidence, 0 meetings, proposals, customers, £0. Re-import appended 0. EXP-ACQ-0001 denominators unchanged (50) |
| Governed reply check | VERIFIED ON REAL STORE · 0 BUYER REPLIES | `age replies check --experiment EXP`. Without a payload it prints the bounded retrieval plan from stored lineage alone: two Gmail searches, both including Trash (replies from the governed recipients; the governed sent threads with every message in them), plus a full-body fetch for snippet-only inbound messages. The CLI holds no Gmail credential; the Gmail connector runs the plan. With the retrieved payload it keeps only governed mail — governed threads, or governed senders outside other experiments' threads, within the window from the first send — and holds back snippet-only human mail so it cannot be captured unreadable. It then runs the existing capture unchanged and refuses the run if any canonical table changes. Summary: threads, inbound, already processed, automated, bounces, reply candidates, ambiguous, pending review, canonical writes, REAL BUYER REPLIES FOUND. No polling, schedule, approval or second adapter. **Real (2026-09-15):** 21 threads / 21 recipients; recipient search 0; the only inbound message is the approved Safe House bounce (already processed); 0 buyer replies, 0 pending, 0 canonical writes on both runs. OEC stays UNKNOWN |
| EXP-ACQ-0006 commercial verdict | VERIFIED LOCAL · REAL: NOT_READY UNTIL 2026-09-22 | `staged_verdict.py` + `age marketing-engineer verdict --experiment-id EXP-ACQ-0006`. It applies the experiment's own gates, transcribed with citations into `experiments/EXP-ACQ-0006/preregistered-gates.json` from FS-001.md, FS-001-A1 and `fs001.py`; the engine holds no thresholds. **ACCESS:** ≥30 desk-eligible, ≥20 named buyers, ≥15 verified-access accounts (DIRECT or route-confirmed), clean-delivery rate ≥70% after 10 observed demand exposures; FAILED only once routing is resolved. **DEMAND:** denominator 30 clean deliveries to named buyers; KILL if replies <5, pain conversations (meeting booked or proposal requested) <3, or proposals <2; PASS on 1 paid design partner. Stage A sends are access attempts, so DEMAND is NOT_ASKED by the protocol's own rule (A1 §5). **Responses:** automated (review ledger, never buyer replies), delivery failure (bounces, outside the delivered denominator), and, from approved canonical data only, routing only (AUTHORITY_SIGNAL), general, demand signal, and qualified conversation (the protocol's pain-conversation definition). **NOT_READY** until the window closes, a reply check runs after it (a new append-only `reply_checks` log) and no reply awaits review. A PASS never waits. Rates stay unset before maturity. Output: POSITIVE / MIXED / NEGATIVE / INCONCLUSIVE, the interpretation, what the result does not establish, and the next decision taken from the preregistration by the funnel's failure point. **Real (2026-09-15):** NOT_READY. 21 attempted, 1 bounced, 20 delivered, 0 matured, historical claim 22 disclosed. ACCESS ROUTING_IN_PROGRESS (named buyers 15 < 20; verified access 1 < 15). DEMAND NOT_ASKED |
| Approved marketing procedures as experimental inputs | VERIFIED LOCAL (synthetic) · REAL: 0 APPROVED EXPORTS, NOT_EVALUABLE | `procedures.py` + `age procedures import / register-internal / bind / retire / validate-result / list` + `age marketing-engineer procedure`. Architecture: `docs/MARKETING_ENGINEER_ARCHITECTURE.md` § Marketing procedures. **Consumes, never admits:** an `approved-skill-export.v1` file from the Intelligent Machine is refused unless the contract and version are supported, admission is APPROVED, the skill is ADOPTED/FORKED and unrevoked, the sha256 is present, provenance is complete (type, commit, licence, repository or URL, approver, date), a marketing use case is permitted, the review is current and the schema holds. Identity is `procedure_id@version` + content hash; registration is KNOWN, never PROVEN, and carries no performance field. ThePlus skills are the seeded baseline, hash-checked against their files. **Declaration:** `experiment_procedures`, append-only, VARIABLE only when the frozen contract says `variable = procedure`, refused after any exposure; a new version is a new experiment. **Evaluator:** preregistered primary metric only (activity metrics refused), per matured delivered exposure through the existing unit model and `change_diagnosis` window resolution; NOT_EVALUABLE / IMMATURE / CONFOUNDED / INSUFFICIENT_SAMPLE (incl. underpowered: smallest visible gap >10 pts) / CONTROLLED_EFFECT / REGRESSION / DESCRIPTIVE_DIFFERENCE / NO_DIFFERENCE; KEEP only on a controlled effect; revenue ASSOCIATED_ONLY unless a controlled experiment's primary metric was paid rate. **Result export:** `skill-evaluation-result.v1`, schema- and claim-validated, authority RECOMMENDATION_ONLY. **Rework 2026-09-15 (cross-repo audit D1/D3/D4):**
- **D1:** a new declaration re-reads the export the procedure was imported from and fails closed. It refuses when the stored review has expired, the export is missing, it is no longer approved or is revoked, the identity or hash changed, or a use case was withdrawn. Historical declarations are untouched.
- **D3:** a result's causal class and revenue claim are re-checked against its own evidence (experiment id, matured sample, competing variables, synthetic flag, z, paid primary metric). The revenue claim is a closed enum.
- **D4:** the import fixture is a real Intelligent Machine export, generated by its own `export_document()` with nested permissions.

**Contract v2 and revocation signal (2026-09-15, after the re-audit):**
- The tightened result contract is published as `skill-evaluation-result.v2`. v1 was restored to its pinned bytes.
- All four shared contracts are pinned by sha256 in both repositories.
- Import accepts only the governed export itself: `<AGE_SKILL_EXPORTS_DIR>/<id>.json`, by default the sibling Intelligent Machine checkout's `contracts/exports/`. A copy under the same shape elsewhere, or a symlink at the governed name, is refused (P2 closed below).
- A new declaration refuses when that file holds the Intelligent Machine's `skill-revocation.v1` notice (`upstream_revoked`). The fixture was generated by the Intelligent Machine's `revocation_notice()`. 33 tests (461 total, gates green); 8 guards mutation-checked from a green baseline: 7 killed on the first pass, 1 survivor (the same-experiment condition, shadowed by the arm-role check) killed after a cross-experiment test was added. **Real (2026-09-15):** the Intelligent Machine has 0 runnable exports (all five AgentVolt candidates DISCOVERED, source unverified), and a DISCOVERED manifest reshaped as an export is REFUSED (not_approved). Seeding loaded 8 baseline procedure rows into the local store; funnel events, evidence, commercial evidence, outbound lineage, reply candidates and experiments unchanged (187 / 46 / 0 / 27 / 12 / 3). `procedure` on EXP-ACQ-0001 and EXP-ACQ-0003 → NOT_EVALUABLE: no procedure declared before exposure, and none is inferred from timing (EXP-ACQ-0006 has events but no contract row, so it reports not found). One offline structural comparison recorded (`docs/PROCEDURE_GAP_REVIEW.md`): public `ab-testing` 2.0.0 vs `theplus.experiment-design` 1.0.0 → REJECT as a replacement, rules mined into `experiment-design` 1.1.0. Not market or revenue evidence |
| `EXP-ACQ-0003` execution | EXECUTING · DESCRIPTIVE_FROZEN_COHORT | Founder, 2026-09-15: the frozen cohort is 39 and cannot reach the preregistered 51, so the experiment runs descriptively on the frozen 39 without rewriting the preregistration (`experiments/EXP-ACQ-0003/EXECUTION-MODE.md`). Verdicts DESCRIPTIVE_POSITIVE / NEGATIVE / INCONCLUSIVE with real-exposure denominators. At recording: 7/39 submitted, 6 delivered and pending, 1 undeliverable, 0 accepted. **Marketing Engineer engineering is paused until this produces outcomes** |
| Vercel edge cache trap | RECORDED | READY is not "users can see it". The apex served the pre-deploy HTML at `x-vercel-cache: HIT, age: 180`, and a cache-busting query did not change the key. Deployment URLs 302 under protection, so the origin check needs `vercel curl` |

## Current experiment freeze

No product features are authorised by `EXP-ACQ-0001`. Work on this experiment is limited to prospect
qualification, teardown research, manual outreach, discovery, proposals, and evidence capture until
its market gate passes. This does not redefine the Digital Marketing Project as an outbound or
cybersecurity project.

## Remediation — independent audit, 2026-09-17

| Phase | Status | Evidence / Notes |
|---|---|---|
| 0 · Public data containment | TESTED | Forward redaction of 46 individuals from HEAD (`e0df2cd`, `f2c8e5a`); identities in git-ignored `private/`; `scripts/pii_guard.py` in `make gate`, 3 mutations KILLED. History not rewritten — founder decision pending |
| 1 · Trust contract | TESTED | No policy is PENDING, never PASS; NOT_APPLICABLE needs a recorded reason; the contract freezes at first treatment (first non-bounced exposure, or a recorded result); `age trust show/declare/not-applicable/observe`. The procedure evaluator's KEEP now passes the same gate. 6 mutations KILLED. Existing experiments migrate to UNDECLARED: EXP-ACQ-0001 and EXP-ACQ-0003 are already in treatment and can never KEEP under this contract; EXP-ACQ-0002 and EXP-ACQ-0007 can still declare before exposure |
| 2A · Idempotency conflicts | TESTED | Same source identity + different fact is refused and audited in append-only `idempotency_conflicts`; the stored event is kept. Stripe amount/currency replays covered. 3 mutations KILLED (`a04d641`) |
| 2B · External timestamps | TESTED | No writer stamps an external fact with the recording time: `invitation-record` keeps the date on file or refuses; `outreach-record --sent-at` required; workbench send/reply take the observed time (the UI asks for it). 5 mutations KILLED. One EXP-ACQ-0003 `not_submitted` invitation still carries a 2026-09-15T07:02:54 recording stamp from the old default — no event, no denominator; left untouched as experiment state |
| 2C · Recipient classification | TESTED | Only an observed class (operator-recorded, or confirmed with `age event-classify`) reaches the staged-verdict, segment or Marketing Engineer denominators. The gmail send import now stores UNKNOWN beside a labelled proposal; word-list classes written earlier read INFERRED_LEGACY. Reviews are append-only. 5 mutations KILLED. EXP-ACQ-0006 outputs identical before/after (demand NOT_ASKED, 0 clean named-buyer deliveries); its 21 sends read INFERRED_LEGACY until reviewed |
| 3 · Evidence immutability | TESTED | `reply_candidates` and `reply_checks` refuse DELETE (UPDATE was already refused), so a pending review cannot be removed to unblock a verdict. A pending reply blocks only the experiment owning its thread; the sender is consulted only for a reply outside every known thread. 4 mutations KILLED. Real pending state unchanged (0 for EXP-ACQ-0001/0003/0006) |

## Engineering backlog — recorded, not fixed

| Defect | Status | Evidence / Notes |
|---|---|---|
| `invitation-record` defaults `submitted_at` to now | FIXED 2026-09-17 (remediation 2B) — kept for the record | `execution.record_invitation` line 153 writes `str(values.get("submitted_at") or _utc_now())`, so recording a LATER lifecycle event (an acceptance) against an existing invitation silently re-dates the exposure. On 2026-09-16 this moved Cheeky Munkey's invitation from 2026-09-15 to today; the importer then corrected the exposure event to match, and refused to restore it (`voided_event_reused`). Recovered append-only: the send date was re-recorded as 2026-09-15 and the exposure re-appended under a new `source_record_id`. Exposure dates feed the maturity window, so a silent default here can corrupt a response window. Fix shape: when the invitation row exists, keep its `submitted_at` unless one is passed explicitly |

## External skill audit gaps closed (2026-09-15, after the independent re-audit)

| Feature | Status | Evidence / Notes |
|---|---|---|
| Result validation selected by the document's own contract | IMPLEMENTED — READY_FOR_INDEPENDENT_REVIEW | `validate_result` picks the v1 or v2 pin from `contract`; anything else is unsupported. The committed example as published at e749c9b (`tests/fixtures/historical_result.v1.json`, byte-identical) validates. A v1 document can never carry a causal class, market validation or KEEP, because v1 records neither competing variables nor synthetic exposure. `ResultContractVersionTests`; 4 guards mutation-checked from green baselines (3 precise kills, 1 kill with blast radius). `make test`: gates green, 487 tests OK |
| Copy-origin claim corrected | SUPERSEDED | The path-shape guard it described was replaced by origin enforcement (next row) |
| P2 — MANAGED_EXPORT_ORIGIN_ENFORCEMENT | IMPLEMENTED — READY_FOR_INDEPENDENT_REVIEW | Closed 2026-09-15 without signing or a server. Import accepts only `governed_export_path(skill_id)`: the resolved `AGE_SKILL_EXPORTS_DIR` (default the sibling Intelligent Machine checkout's `agentic-os/external-skills/contracts/exports`) plus `<skill_id>.json`, the one file its revocation notice is written over. A copy under the same shape elsewhere and a symlink at the governed name are refused (`not_the_published_export`). A new declaration re-checks the stored origin against the governed path and refuses a procedure imported anywhere else (`untrusted_export_origin`); history is untouched. The known-limit test that asserted the copy outlived revocation was rewritten on purpose as `test_a_copy_under_the_published_shape_is_refused_so_it_cannot_outlive_revocation`; plus `test_a_procedure_no_longer_at_the_governed_origin_cannot_be_declared_again` |
